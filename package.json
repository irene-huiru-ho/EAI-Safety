{
  "name": "home-safety-study",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "NODE_ENV=production node server/src/index.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^118.0.0",
    "multer": "^2.1.1",
    "uuid": "^9.0.1"
  }
}
